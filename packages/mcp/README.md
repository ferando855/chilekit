# @chilekit/mcp

Servidor MCP stdio para ChileKit.

Para agentes simples, prefiere el CLI:

```bash
chilekit search "ipc abril 2026" --json
```

Usa MCP cuando el cliente ya tenga buen soporte para tools persistentes:

```bash
chilekit mcp
```
