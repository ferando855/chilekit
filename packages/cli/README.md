# @chilekit/cli

CLI principal de ChileKit.

Pensado para humanos y agentes:

- Sin prompts interactivos.
- `--json` para salida estructurada.
- Errores por `stderr`.
- Exit code `0` para exito, `1` para errores operacionales.
